# Production Environment Configuration
# This file is used when NODE_ENV=production

# All sensitive variables (Supabase keys, VPS credentials, LLM gateway keys)
# should be set in your deployment environment (Vercel project settings)
# for security. Do not commit them here.