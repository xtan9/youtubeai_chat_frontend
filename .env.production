# Production Environment Configuration
# This file is used when NODE_ENV=production

# API Configuration for Production
NEXT_PUBLIC_API_URL=https://api.youtubeai.chat

# Production optimizations
# All other variables should be set in your deployment environment
# (Vercel, Netlify, etc.) for security