# Prepare and remember AI Recommendation Assessments

AI pair assessment runs in the background only after internal semantic retrieval and deterministic admission have narrowed the candidate pool. Its structured result is stored as a versioned Recommendation Assessment and materialized into a Recommendation Set, trading some freshness and background complexity for reusable external work, predictable user-facing latency, and explanations that can be audited against their evidence.
